# ── app/ — Basic NL-to-SQL chatbot ──────────────────────────────────────────
streamlit
langchain-ollama
langchain-core
python-dotenv
requests

# ── app2/ — Vector search NL-to-SQL (23-table university library DB) ────────
sentence-transformers==5.5.0
scikit-learn==1.8.0
torch==2.0.1+cpu
numpy==1.26.4
transformers==4.46.3

# ── app3/ — MCP implementation + FastAPI server (needs Python 3.11) ─────────
openai
mcp
fastapi
uvicorn[standard]
psycopg2-binary
