FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir fastapi uvicorn psycopg2-binary openai mcp python-dotenv

COPY . .

RUN python app3/migrate_postgres.py || true

CMD ["uvicorn", "app3.server:app", "--host", "0.0.0.0", "--port", "8000"]
